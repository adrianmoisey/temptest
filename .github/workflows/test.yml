name: Print date

on:
  push:
    branches:
      - master
  pull_request:


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Print date
      run: date

    - name: Print mount
      run: mount

